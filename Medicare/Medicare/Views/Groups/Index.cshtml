@model IEnumerable<Medicare.Models.Groups>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupsId })
        </td>
    </tr>
}

</table>
